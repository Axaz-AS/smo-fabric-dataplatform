version: 2

models:
  - name: int_nobs__nomenklatur_detail
    description: "Active NOBS nomenclature/product detail rows — staging data with KPL soft-deleted rows removed. Pass-through of src_nobs__statnomenklatur_detail; no further business logic."
    config:
      meta:
        grain: one row per active nomenclature product detail row
        source_system: nobs
    columns:
      - name: pk_nomenklatur_detail_id
        description: "KPL record id of the nomenclature detail row."
        tests: [unique, not_null]
